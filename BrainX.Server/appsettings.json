{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  "//BrainX": "Node config. Env overrides (BrainX__Key). Defaults = embedded/local; the standalone node (Docker/VPS) sets EmbeddedMode=false + RequireAuth=true + BearerToken + AllowedOrigins.",
  "BrainX": {
    "VaultPath": "",
    "EmbeddedMode": true,
    "RequireAuth": false,
    "BearerToken": "",
    "Urls": "",
    "AllowedOrigins": ""
  }
}
